<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="c971_project.Views.AddCoursePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:system="clr-namespace:System;assembly=netstandard"
    Title="Add Course"
    BackgroundColor="{StaticResource Cream}">
    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="15">

                <!--  Header  -->
                <Label
                    Margin="0,20,0,0"
                    HorizontalOptions="Center"
                    Style="{StaticResource HeaderLabel}"
                    Text="New Course" />

                <!--  Card container  -->
                <Border
                    Margin="0,0,0,100"
                    Padding="20"
                    BackgroundColor="White"
                    Stroke="{StaticResource Charcoal}"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="3">

                    <Border.Shadow>
                        <Shadow
                            Brush="{StaticResource DarkBlue}"
                            Opacity="0.15"
                            Radius="8"
                            Offset="0,4" />
                    </Border.Shadow>

                    <VerticalStackLayout>
                        <Label
                            Margin="0,20,0,10"
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="Course Information"
                            TextColor="{StaticResource DarkBlue}" />

                        <!--  Course Name  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Name *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Placeholder="Enter course name"
                                    Text="{Binding NewCourse.Name, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>

                        <!--  Course Number  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Number *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Placeholder="e.g., MATH101"
                                    Text="{Binding NewCourse.CourseNum, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>

                        <!--  Credit Units  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Credit Units *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Picker
                                    Title="Select credit units"
                                    ItemsSource="{Binding CreditUnitOptions}"
                                    SelectedItem="{Binding NewCourse.CuNum, Mode=TwoWay}">
                                    <Picker.ItemDisplayBinding>
                                        <Binding Path="." />
                                    </Picker.ItemDisplayBinding>
                                </Picker>
                            </Border>
                        </VerticalStackLayout>

                        <!--  Start Date  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Start Date"
                                TextColor="{StaticResource DarkBlue}" />

                            <Grid
                                ColumnDefinitions="Auto,*,Auto,*"
                                ColumnSpacing="10"
                                VerticalOptions="Center">
                                <!--  Calendar Icon  -->
                                <Image
                                    Grid.Column="0"
                                    HeightRequest="20"
                                    Source="calendar_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  DatePicker  -->
                                <DatePicker
                                    Grid.Column="1"
                                    Date="{Binding NewCourse.StartDate, Mode=TwoWay}"
                                    HorizontalOptions="Fill" />

                                <!--  Alarm Icon  -->
                                <Image
                                    Grid.Column="2"
                                    HeightRequest="20"
                                    Source="alarm_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  TimePicker  -->
                                <TimePicker
                                    Grid.Column="3"
                                    HorizontalOptions="Fill"
                                    Time="{Binding NewCourse.StartTime, Mode=TwoWay}" />
                            </Grid>
                        </VerticalStackLayout>

                        <!--  Start Date Notification Toggle  -->
                        <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                            <Switch
                                IsToggled="{Binding NewCourse.NotifyStartDate, Mode=TwoWay}"
                                OnColor="{StaticResource DarkBlue}"
                                ThumbColor="{StaticResource Cream}"
                                VerticalOptions="Center" />
                            <Label
                                Text="{Binding NewCourse.StartDateTime, StringFormat='Notify me on Start Date ({0:MMM dd, yyyy h:mm tt})'}"
                                TextColor="{StaticResource DarkBlue}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>

                        <!--  End Date  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="End Date"
                                TextColor="{StaticResource DarkBlue}" />

                            <Grid
                                ColumnDefinitions="Auto,*,Auto,*"
                                ColumnSpacing="10"
                                VerticalOptions="Center">
                                <!--  Calendar Icon  -->
                                <Image
                                    Grid.Column="0"
                                    HeightRequest="20"
                                    Source="calendar_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  DatePicker  -->
                                <DatePicker
                                    Grid.Column="1"
                                    Date="{Binding NewCourse.EndDate, Mode=TwoWay}"
                                    HorizontalOptions="Fill" />

                                <!--  Alarm Icon  -->
                                <Image
                                    Grid.Column="2"
                                    HeightRequest="20"
                                    Source="alarm_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  TimePicker  -->
                                <TimePicker
                                    Grid.Column="3"
                                    HorizontalOptions="Fill"
                                    Time="{Binding NewCourse.EndTime, Mode=TwoWay}" />
                            </Grid>
                        </VerticalStackLayout>

                        <!--  End Date Notification Toggle  -->
                        <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                            <Switch
                                IsToggled="{Binding NewCourse.NotifyEndDate, Mode=TwoWay}"
                                OnColor="{StaticResource DarkBlue}"
                                ThumbColor="{StaticResource Cream}"
                                VerticalOptions="Center" />
                            <Label
                                Text="{Binding NewCourse.EndDateTime, StringFormat='Notify me on End Date ({0:MMM dd, yyyy h:mm tt})'}"
                                TextColor="{StaticResource DarkBlue}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>

                        <!--  INSTRUCTOR INFORMATION SECTION  -->
                        <Label
                            Margin="0,40,0,10"
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="Instructor Information"
                            TextColor="{StaticResource DarkBlue}" />

                        <!--  Instructor Name  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Name *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Placeholder="Enter instructor name"
                                    Text="{Binding NewInstructor.Name, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>

                        <!--  Instructor Email  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Email *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Keyboard="Email"
                                    Placeholder="instructor@school.edu"
                                    Text="{Binding NewInstructor.Email, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>

                        <!--  Instructor Phone  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Phone *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Keyboard="Telephone"
                                    Placeholder="(555) 123-4567"
                                    Text="{Binding NewInstructor.Phone, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>

        <!--  Floating Save Button  -->
        <HorizontalStackLayout
            Padding="16"
            HorizontalOptions="End"
            InputTransparent="False"
            Spacing="12"
            VerticalOptions="End">

            <!--  Label  -->
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="Save Course"
                TextColor="{StaticResource DarkBlue}"
                VerticalOptions="Center" />

            <!--  Circular Button  -->
            <Border
                BackgroundColor="{StaticResource DarkBlue}"
                HeightRequest="55"
                StrokeShape="Ellipse"
                WidthRequest="55">
                <Border.Shadow>
                    <Shadow
                        Brush="{StaticResource DarkBlue}"
                        Opacity="0.5"
                        Radius="8"
                        Offset="0,4" />
                </Border.Shadow>

                <Image
                    HeightRequest="45"
                    HorizontalOptions="Center"
                    Source="check_icon.svg"
                    VerticalOptions="Center"
                    WidthRequest="45">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SaveCourseCommand}" />
                    </Image.GestureRecognizers>
                </Image>
            </Border>
        </HorizontalStackLayout>
    </Grid>
</ContentPage>