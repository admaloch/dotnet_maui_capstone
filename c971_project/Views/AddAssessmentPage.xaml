<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="c971_project.Views.AddAssessmentPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:System="clr-namespace:System;assembly=mscorlib"
    xmlns:system="clr-namespace:System;assembly=netstandard"
    Title="Add Assessment"
    BackgroundColor="{StaticResource Cream}">
    <Grid>
        <!--  Full screen loading overlay  -->
        <Grid
            BackgroundColor="#80000000"
            HorizontalOptions="Fill"
            IsVisible="{Binding IsBusy}"
            VerticalOptions="Fill"
            ZIndex="1000">

            <ActivityIndicator
                HeightRequest="60"
                HorizontalOptions="Center"
                IsRunning="True"
                VerticalOptions="Center"
                WidthRequest="60"
                Color="White" />
        </Grid>
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="15">

                <!--  Header  -->
                <Border
                    Margin="0,10,0,0"
                    Padding="20,10"
                    BackgroundColor="{StaticResource DarkBlue}"
                    HorizontalOptions="Center"
                    Stroke="{StaticResource LightBrown}"
                    StrokeShape="RoundRectangle 10"
                    StrokeThickness="2">

                    <Label
                        BackgroundColor="Transparent"
                        HorizontalOptions="Center"
                        HorizontalTextAlignment="Center"
                        Style="{StaticResource HeaderLabel}"
                        Text="Assessment Information"
                        TextColor="{StaticResource Cream}" />
                </Border>


                <!--  Card container  -->
                <Border
                    Margin="0,0,0,70"
                    Padding="20"
                    BackgroundColor="{StaticResource LightGray}"
                    Stroke="{StaticResource Charcoal}"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="3">

                    <Border.Shadow>
                        <Shadow
                            Brush="{StaticResource DarkBlue}"
                            Opacity="0.15"
                            Radius="8"
                            Offset="0,4" />
                    </Border.Shadow>

                    <VerticalStackLayout Spacing="15">

                        <!--  Assessment Name  -->
                        <VerticalStackLayout Spacing="1">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Name *"
                                TextColor="{StaticResource DarkBlue}" />
                            <Border BackgroundColor="White" StrokeShape="RoundRectangle 8">
                                <Entry
                                    BackgroundColor="Transparent"
                                    Placeholder="Enter assessment name"
                                    Text="{Binding NewAssessment.Name, Mode=TwoWay}"
                                    TextColor="{StaticResource DarkBlue}" />
                            </Border>
                        </VerticalStackLayout>

                        <!--  Assessment Type  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Assessment Type"
                                TextColor="{StaticResource DarkBlue}" />
                            <Picker
                                Title="Select type"
                                BackgroundColor="{StaticResource White}"
                                ItemsSource="{Binding TypeOptions}"
                                SelectedItem="{Binding NewAssessment.Type, Mode=TwoWay}"
                                TextColor="{StaticResource DarkBlue}" />
                        </VerticalStackLayout>

                        <!--  Assessment Status  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Status"
                                TextColor="{StaticResource DarkBlue}" />
                            <Picker
                                Title="Select status"
                                BackgroundColor="{StaticResource White}"
                                ItemsSource="{Binding StatusOptions}"
                                SelectedItem="{Binding NewAssessment.Status, Mode=TwoWay}"
                                TextColor="{StaticResource DarkBlue}" />
                        </VerticalStackLayout>

                        <!--  Start Date  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="Start Date"
                                TextColor="{StaticResource DarkBlue}" />

                            <Grid
                                ColumnDefinitions="Auto,*,Auto,*"
                                ColumnSpacing="10"
                                VerticalOptions="Center">
                                <!--  Calendar Icon  -->
                                <Image
                                    Grid.Column="0"
                                    HeightRequest="20"
                                    Source="calendar_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  DatePicker  -->
                                <DatePicker
                                    Grid.Column="1"
                                    BackgroundColor="{StaticResource White}"
                                    Date="{Binding NewAssessment.StartDate, Mode=TwoWay}"
                                    HorizontalOptions="Fill"
                                    MinimumDate="{x:Static system:DateTime.Today}"
                                    TextColor="{StaticResource DarkBlue}" />

                                <!--  Alarm Icon  -->
                                <Image
                                    Grid.Column="2"
                                    HeightRequest="20"
                                    Source="alarm_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  TimePicker  -->
                                <TimePicker
                                    Grid.Column="3"
                                    BackgroundColor="{StaticResource White}"
                                    HorizontalOptions="Fill"
                                    TextColor="{StaticResource DarkBlue}"
                                    Time="{Binding NewAssessment.StartTime, Mode=TwoWay}" />
                            </Grid>
                        </VerticalStackLayout>

                        <!--  Start Date Notification Toggle  -->
                        <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                            <Switch
                                IsToggled="{Binding NewAssessment.NotifyStartDate, Mode=TwoWay}"
                                OnColor="{StaticResource DarkBlue}"
                                ThumbColor="{StaticResource Cream}"
                                VerticalOptions="Center" />
                            <Label
                                Text="{Binding NewAssessment.StartDateTime, StringFormat='Notify me on Start Date'}"
                                TextColor="{StaticResource DarkBlue}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>

                        <!--  End Date  -->
                        <VerticalStackLayout Spacing="5">
                            <Label
                                Style="{StaticResource FieldLabel}"
                                Text="End Date"
                                TextColor="{StaticResource DarkBlue}" />

                            <Grid
                                ColumnDefinitions="Auto,*,Auto,*"
                                ColumnSpacing="10"
                                VerticalOptions="Center">
                                <!--  Calendar Icon  -->
                                <Image
                                    Grid.Column="0"
                                    HeightRequest="20"
                                    Source="calendar_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  DatePicker  -->
                                <DatePicker
                                    Grid.Column="1"
                                    BackgroundColor="{StaticResource White}"
                                    Date="{Binding NewAssessment.EndDate, Mode=TwoWay}"
                                    HorizontalOptions="Fill"
                                    MinimumDate="{x:Static system:DateTime.Today}"
                                    TextColor="{StaticResource DarkBlue}" />

                                <!--  Alarm Icon  -->
                                <Image
                                    Grid.Column="2"
                                    HeightRequest="20"
                                    Source="alarm_icon.svg"
                                    VerticalOptions="Center"
                                    WidthRequest="20" />

                                <!--  TimePicker  -->
                                <TimePicker
                                    Grid.Column="3"
                                    BackgroundColor="{StaticResource White}"
                                    HorizontalOptions="Fill"
                                    TextColor="{StaticResource DarkBlue}"
                                    Time="{Binding NewAssessment.EndTime, Mode=TwoWay}" />
                            </Grid>
                        </VerticalStackLayout>

                        <!--  End Date Notification Toggle  -->
                        <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                            <Switch
                                IsToggled="{Binding NewAssessment.NotifyEndDate, Mode=TwoWay}"
                                OnColor="{StaticResource DarkBlue}"
                                ThumbColor="{StaticResource Cream}"
                                VerticalOptions="Center" />
                            <Label
                                Text="{Binding NewAssessment.EndDateTime, StringFormat='Notify me on End Date'}"
                                TextColor="{StaticResource DarkBlue}"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>

                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>

        <!--  Floating Save Button  -->
        <HorizontalStackLayout
            Padding="16"
            HorizontalOptions="End"
            InputTransparent="False"
            Spacing="12"
            VerticalOptions="End">

            <!--  Label  -->
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="Create Assessment"
                TextColor="{StaticResource DarkBlue}"
                VerticalOptions="Center" />

            <!--  Circular Button  -->
            <Border
                BackgroundColor="{StaticResource DarkBlue}"
                HeightRequest="55"
                StrokeShape="Ellipse"
                WidthRequest="55">
                <Border.Shadow>
                    <Shadow
                        Brush="{StaticResource DarkBlue}"
                        Opacity="0.5"
                        Radius="8"
                        Offset="0,4" />
                </Border.Shadow>

                <Image
                    HeightRequest="45"
                    HorizontalOptions="Center"
                    Source="check_icon.svg"
                    VerticalOptions="Center"
                    WidthRequest="45">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding SaveAssessmentCommand}" />
                    </Image.GestureRecognizers>
                </Image>
            </Border>
        </HorizontalStackLayout>
    </Grid>
</ContentPage>